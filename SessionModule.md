Код для модуля сеанса

```
Процедура УстановкаПараметровСеанса(ТребуемыеПараметры)     
	
	ДанныеПользователя=ПользователиИнформационнойБазы.ТекущийПользователь();
	Пользователь=Справочники.Пользователи.НайтиПоНаименованию(ДанныеПользователя.ПолноеИмя);
	Если Пользователь=Неопределено Тогда  
		НоваяЗапись=Справочники.Пользователи.СоздатьЭлемент();
		НоваяЗапись.Наименование=ДанныеПользователя.ПолноеИмя;
		НоваяЗапись.Логин=ДанныеПользователя.Имя;
		НоваяЗапись.Пароль=ДанныеПользователя.Пароль;
		НоваяЗапись.Роль=ДанныеПользователя.Роли;
		НоваяЗапись.УникальныйИдентификатор=ДанныеПользователя.УникальныйИдентификатор;
		НоваяЗапись.Записать();
		Пользователь=НоваяЗапись.Ссылка;
	КонецЕсли;
	
	
	ПараметрыСеанса.ТекущийПользователь=Пользователь;
			
КонецПроцедуры

```
